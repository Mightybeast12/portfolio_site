steps:
  # Step 1: Install Rust and Trunk
  - name: 'rust:latest'
    entrypoint: bash
    args:
      - '-c'
      - |
        rustup target add wasm32-unknown-unknown
        cargo install trunk
        trunk build --release
        
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/yew-app:$COMMIT_SHA', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/yew-app:$COMMIT_SHA']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - run
      - deploy
      - yew-app
      - --image
      - 'cloud-run-source-deploy/$PROJECT_ID/yew-app:$COMMIT_SHA'
      - --region
      - europe=west9
      - --platform
      - managed
      - --allow-unauthenticated

images:
  - 'cloud-run-source-deploy/yew-app:$COMMIT_SHA'
options:
  logging: CLOUD_LOGGING_ONLY
