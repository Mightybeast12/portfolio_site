name: Test Terraform Backend

on:
  workflow_dispatch:
  push:
    branches: [test-backend]

env:
  TF_VERSION: "1.5.0"
  TF_WORKING_DIR: "./terraform"

jobs:
  test-backend:
    name: "Test GCS Backend"
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Test Terraform Init with Backend
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          echo "🧪 Testing Terraform backend initialization..."
          terraform init
          echo "✅ Backend initialization successful!"

      - name: Test State Access
        run: |
          echo "🧪 Testing state file access..."
          terraform state list || echo "No resources in state yet"
          echo "✅ State access successful!"

      - name: Verify State Location
        run: |
          echo "🧪 Verifying state is stored in GCS..."
          gsutil ls gs://portfolio-site-434710-terraform-state/terraform/state/ || echo "State file not found in GCS"
          echo "✅ State verification complete!"

      - name: Test Summary
        run: |
          echo "## 🧪 Backend Test Results" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Terraform init with GCS backend" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ State file access working" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ GCS bucket accessible" >> $GITHUB_STEP_SUMMARY